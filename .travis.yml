language:
  - python

dist:
  - xenial

python:
  - 3.7

install:
  - pip install pipenv
  - pipenv install --dev

script:
  - export PIPENV_VERBOSITY=-1
  - export BOTO_CONFIG=/dev/null
  - pip list
  - pipenv run python -m pytest
